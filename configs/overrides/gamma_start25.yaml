train:
  roi_bank_gamma:
    enabled: true
    update_interval_epochs: 25
    start_epoch: 25
    stop_epoch: 300
    output_dir: roi_bank_gamma_start25
metadata:
  training_mode: neptune_v03_microtube_roi_gamma_start25
